'use client';

import { styled } from 'styled-components';

export const MagneticItem = styled.p`
   position: relative;
   z-index: 1;
   display: block;
   width: max-content;
   word-break: break-all;
`;
// min-width: 14ch;
